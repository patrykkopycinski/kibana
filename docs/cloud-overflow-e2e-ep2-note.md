# Cloud-overflow e2e EP2 note (disposable)

Open-PR leg of the Phase 5 end-to-end validation: this PR is deliberately left
OPEN so the reaper pass can prove that a non-merged PR does NOT revoke the
per-VM deploy key (D9's merge trigger must fire only on a merged PR).
