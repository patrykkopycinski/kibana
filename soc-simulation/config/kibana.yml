# SOC Simulation — Kibana configuration overrides
# Note: elasticsearch.hosts, credentials, fleet settings come from environment variables
# in docker-compose.yml. This file only adds settings not easily set via env vars.

# Enable experimental Agent Builder features (SML, skills)
uiSettings:
  overrides:
    agentBuilder:experimentalFeatures: true
    agentBuilder:navEnabled: true
