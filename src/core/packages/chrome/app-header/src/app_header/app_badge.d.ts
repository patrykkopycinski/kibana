import React from 'react';
import type { AppHeaderBadge } from '../types';
export declare function AppBadge({ badge }: {
    badge: AppHeaderBadge;
}): React.JSX.Element;
export declare namespace AppBadge {
    var displayName: string;
}
