version: '1'
name: SOC Response
description: Deterministic containment actions based on confidence thresholds.
enabled: true

triggers:
  - type: manual

inputs:
  - name: context
    type: string

steps:
  - name: log_response
    type: console
    with:
      message: "SOC Response action executed: {{ inputs.context }}"
