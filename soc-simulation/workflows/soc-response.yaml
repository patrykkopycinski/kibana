version: '1'
name: SOC Response
description: Deterministic containment actions (no AI). Logs actions, writes to audit trail, chains to Case Creation.

consts:
  response_actions_index: ".soc-response-actions"
  audit_index: ".soc-audit-trail"
  outcomes_index: ".soc-outcomes"

enabled: true

triggers:
  - type: manual

inputs:
  - name: investigation_result
    type: string
  - name: alert_data
    type: string

steps:
  - name: log_response_start
    type: console
    with:
      message: "SOC Response initiated for: {{ inputs.investigation_result }}"

  - name: write_response_action
    type: elasticsearch.index
    with:
      index: "{{ consts.response_actions_index }}"
      document:
        "@timestamp": "{{ 'now' | date: '%Y-%m-%dT%H:%M:%SZ' }}"
        action_type: "log_and_escalate"
        source: "soc-response"
        investigation_result: "{{ inputs.investigation_result }}"
        status: "completed"

  - name: write_audit
    type: elasticsearch.index
    with:
      index: "{{ consts.audit_index }}"
      document:
        "@timestamp": "{{ 'now' | date: '%Y-%m-%dT%H:%M:%SZ' }}"
        workflow_id: "soc-response"
        event_type: "response_action"
        source: "soc-response"
        details: "Deterministic response logged for escalated alert"
        status: "completed"

  - name: write_outcome
    type: elasticsearch.index
    with:
      index: "{{ consts.outcomes_index }}"
      document:
        "@timestamp": "{{ 'now' | date: '%Y-%m-%dT%H:%M:%SZ' }}"
        correlation_id: "response-{{ 'now' | date: '%Y-%m-%dT%H:%M:%SZ' }}"
        disposition: "responded"
        agents_involved:
          - default
          - soc-entity-agent
          - soc-hunt-agent

  - name: log_response_done
    type: console
    with:
      message: "SOC Response actions completed, chaining to case creation"
