# Tasks

## Phase 0 — Foundation (No Offensive Code)

- [ ] Create Kibana plugin scaffold (`x-pack/plugins/security_emulation`) with `kibana.jsonc`, `plugin.ts`, server/public entry points (Closes: E2, E4; Risk mitigation: S3, S5)
- [ ] Define and register `emulation:read` and `emulation:execute` feature privileges in Kibana RBAC system (Closes: S1; Risk mitigation: S1)
- [ ] Create saved-object types: `emulation-report`, `emulation-host-allowlist`, `emulation-cached-scenario` with space-scoped schemas (Closes: S5, P4; Risk mitigation: S5)
- [ ] Create `.kibana-security-emulation-logs-<spaceId>-*` index template with ECS schema, 7-day default ILM delete policy, and user-configurable per-space `max_size` cap (Closes: S6, O3; Risk mitigation: S6, O3)
- [ ] Create `.kibana-security-emulation-history-<spaceId>-*` index template with versioned mapping (schema: emulation_id, rule_id, scenario_fingerprint, mode, target_hosts, score, tp_count, fp_count, phase_outcomes, created_at, operator_id, space_id), 90-day default ILM delete policy, user-configurable per space (Closes: O3, P4; Risk mitigation: O3, P4)
- [ ] Implement `AuditLogger` component wrapping `core.auditTrail` to emit `kibana.security.emulation.action` events (fields: operator_id, target_host_id, command_hash_sha256, rule_id, mode, emulation_id, outcome, error_message) (Closes: S4; Risk mitigation: S4)
- [ ] Implement `RBACGuard` middleware enforcing `emulation:read` and conditional `emulation:execute` privilege checks; return HTTP 403 with typed error codes (Closes: S1; Risk mitigation: S1)
- [ ] Create `FeatureFlagRegistry` class exposing `detectionEmulation.{foundation, logInjection, realExecution, skillComposition, migrationValidator}` flags; unit test asserts all flags are checked before component initialization (Closes: all phases; Risk mitigation: scoped rollout)
- [ ] Implement CI lint rule that scans workflow YAML registry and all TypeScript imports for patterns: `Authorization: Basic`, `Authorization: Bearer`, `default: "20\d\d-`, and `import.*internal/` (Closes: S2, O8, E2; Risk mitigation: S2, O8, E2)
- [ ] Register skill in Agent Builder catalog with card title "Detection Emulation", description from proposal "What Changes", and `validateRule(rule, hosts, options) -> ValidationReport` API contract (Closes: none; Risk mitigation: E4)
- [ ] Create HTTP routes (`POST /api/security_emulation/v1/validate_rule`, `GET /api/security_emulation/v1/history/<emulationId>`) wrapped by `RBACGuard` and feature-flag checks (Closes: S3, E2; Risk mitigation: S3, E2)
- [ ] Write unit tests for RBAC guard, feature-flag registry, audit logger, and saved-object schemas asserting space-scoping and field validation (Closes: all; Risk mitigation: S1, S4, S5)

## Phase 1 — Log Injection Mode

- [ ] Implement `ScenarioGenerator` component: extract MITRE technique IDs from rule, query ELSER-2-indexed scenario templates by technique similarity (degrade to BM25 with "fewer semantic results" notice if ELSER unavailable), assemble directed attack graph (phases with command templates, expected ECS fields, dependencies), generate deterministic SHA-256 fingerprint, return cached scenario if fingerprint matches previous execution (Closes: E1, P6; Risk mitigation: E1, P6)
- [ ] Implement `LogInjectionExecutor` component: walk attack graph, hydrate ECS document templates with phase-specific values (timestamp, host, user, process tree per rule's data-source schema), batch-write to `.kibana-security-emulation-logs-<spaceId>-*` with `event.dataset: "kibana.security.emulation"` and `kibana.emulation.id: <emulationId>` tags, verify target rule's `index` array includes emulation index pattern or return HTTP 400 (error code `EMULATION_INDEX_OPT_IN_MISSING`), stream phase-completion events to caller (Closes: S6, O1, O2, P3; Risk mitigation: S6, O1, O2, P3)
- [ ] Implement `TelemetryCollector` component: poll `/api/detection_engine/rules/<ruleId>/alerts` with filter `(kibana.alert.emulation.id:<emulationId>)` during emulation time window, aggregate TP (alerts matching expected phases) and FP (alerts outside expected phases), compute per-technique coverage, return structured metrics (Closes: none; Risk mitigation: none)
- [ ] Implement `ConfidenceScorer` component: apply weighted formula `score = (TP_coverage × 0.5) + (precision × 0.3) + (determinism × 0.2)` where precision = TP / (TP + FP), determinism = 1 - (stddev latency / mean latency), clamp to [0, 1], annotate with mode-specific caveats (Closes: P3; Risk mitigation: P3)
- [ ] Implement `EmulationHistoryStore` component: write reports to `.kibana-security-emulation-history-<spaceId>-*`, support semantic search via ELSER-2 for regression comparison with BM25 fallback, enforce 90-day ILM policy (Closes: O3, E1; Risk mitigation: O3, E1)
- [ ] Implement HTTP handler for `POST /api/security_emulation/v1/validate_rule` accepting `{ rule, targetHosts, mode?: "log_injection", fresh?: boolean }`, enforcing Log Injection opt-in check (S6), streaming phase progress, returning `ValidationReport` JSON (Closes: S6, P3; Risk mitigation: S6)
- [ ] Implement `returnValidationReport()` function returning schema: `{ emulationId, ruleId, mode, targetHosts, confidenceScore, truePositives, falsePositives, phases: [{ id, technique, status, alertIds, errorMessage?, duration_ms }], startTime, endTime, status: "completed" | "partial" | "timeout" | "error" }` (Closes: none; Risk mitigation: none)
- [ ] Create `attack_graph_templates.yaml` registry in `x-pack/plugins/security_emulation/server/workflows/` with sample MITRE technique templates (T1059.001, T1547.001, T1543.003) demonstrating ECS field mappings and phase dependencies; CI lint asserts no hardcoded secrets (Closes: S2, O8; Risk mitigation: S2, O8)
- [ ] Write integration tests: (1) Log Injection emulation against mock Detection Engine rule, (2) index-pattern opt-in enforcement, (3) TP/FP scoring with synthetic alerts, (4) emulation history persistence, (5) ELSER-2 semantic search with BM25 fallback (Closes: S6, O1, O2, P3, E1; Risk mitigation: all)
- [ ] Update Detection Engine UI to surface "EMULATION" badge on alerts with `kibana.alert.emulation.id` set; add "Emulation alerts" filter to default analyst triage view (Closes: O1, P2; Risk mitigation: O1, P2)
- [ ] Write user-facing README in plugin documenting: Log Injection mode operation, index-pattern opt-in requirement, TP/FP scoring formula, mode caveats, emulation history search (Closes: none; Risk mitigation: none)

## Phase 2 — Real Execution Mode

- [ ] Implement `HostAllowlistManager` saved-object CRUD: per-space allowlist entries (fields: host_id, enrolled_at, enrolled_by, enrolled_for_user_id, expires_at), enforce annual re-enrollment check, return 403 (error code `EMULATION_HOST_NOT_ALLOWED`) if host missing or expired (Closes: S7; Risk mitigation: S7)
- [ ] Implement SOC ops console UI for enrolling hosts: host selector dropdown (populated from Elastic Defend agent list), expiry picker (default 365 days), re-enrollment nag for entries expiring within 30 days (Closes: S7; Risk mitigation: S7)
- [ ] Implement `RealExecutionExecutor` component: check `emulation:execute` privilege or return 403 (error code `EMULATION_PRIVILEGE_MISSING`), validate target hosts against space allowlist via `HostAllowlistManager`, acquire host-level lock keyed by `(ruleId, hostId)` or return 409 (error code `EMULATION_CONFLICT`), call `/api/endpoint/action/execute` with structured command args (never free-text), emit audit-log events via `AuditLogger`, enforce per-phase rate limit (default 10 cmd/host/min, hard ceiling 100), honor wall-clock budget (default 30 min, hard ceiling 2 h), release lock on completion/timeout/error, stream command results to caller (Closes: S1, S7, O4, O6, O7; Risk mitigation: S1, S7, O4, O6, O7)
- [ ] Implement `PublicAPIAdapter` wrapper: for every Elastic Defend or Detection Engine `internal/` API call (`/internal/alerting/rules/backfill/_schedule`, `/internal/detection_engine/...`), create a versioned public route (`/api/security_emulation/v1/<route>`) if missing; CI lint fails build on `import.*internal/` pattern detection in skill code (Closes: S3, E2; Risk mitigation: S3, E2)
- [ ] Implement LLM-driven command-confirmation flow: in interactive mode, render the LLM-suggested command to the operator with approval button; in programmatic mode, require policy flag `allowAutoExecute: false` (default deny) to auto-execute (Closes: S8; Risk mitigation: S8)
- [ ] Implement per-emulation "Suppress detection? Y/N" toggle in SOC ops console: default Y for `low` and `medium` severity rules, default N for `high` and `critical`; auto-expire suppressions when emulation ends (Closes: O5; Risk mitigation: O5)
- [ ] Update HTTP handler for `POST /api/security_emulation/v1/validate_rule` to support `mode: "real_execution"`: enforce privilege + allowlist checks, acquire host lock, run `RealExecutionExecutor`, return partial results on timeout (Closes: S1, S7, O4, O6; Risk mitigation: S1, S7, O4, O6)
- [ ] Implement hard plan-size ceiling check: reject emulation with HTTP 400 (error code `EMULATION_PLAN_TOO_LARGE`) if plan exceeds 20 phases or 50 target hosts (Closes: O6; Risk mitigation: O6)
- [ ] Implement wall-clock timeout handler: cancel in-flight Defend commands on budget exceeded, return `ValidationReport` with `status: "timeout"` and partial phase results (Closes: O6, E6; Risk mitigation: O6, E6)
- [ ] Gate Real Execution mode to `false` by default on Elastic Cloud Serverless; document Legal sign-off requirement for Cloud GA (Closes: L1; Risk mitigation: L1)
- [ ] Write integration tests: (1) Real Execution with mocked Defend API calls, (2) RBAC privilege enforcement (403 on missing `emulation:execute`), (3) host allowlist validation (403 on missing/expired host), (4) host-level locking (409 on concurrent emulation), (5) per-phase rate limiting, (6) wall-clock timeout and partial result return, (7) audit-log emission with correct fields (Closes: S1, S7, O4, O6, O7; Risk mitigation: all)
- [ ] Write user-facing README additions: Real Execution mode operation, host enrollment and annual re-enrollment, privilege requirements, suppression toggle behavior, rate limits, wall-clock budget, mode caveats (Closes: none; Risk mitigation: none)
- [ ] Display persistent UI banner during Real Execution showing current mode label ("Real Execution") and wall-clock countdown timer (Closes: P1; Risk mitigation: P1)

## Phase 3 — Skill Composition with Detection Engineering

- [ ] Define public API contract for Detection Engineering Skill: `validateRule(rule, targetHosts, options?) -> ValidationReport` function callable by Detection Engineering Skill workflow, accepting optional `{ mode, fresh, autoConfirm }` flags (Closes: none; Risk mitigation: none)
- [ ] Implement HTTP endpoint `POST /api/security_emulation/v1/validate_rule/async` returning `{ jobId, statusUrl }` for long-running emulations; caller polls `GET /api/security_emulation/v1/jobs/<jobId>` for streaming progress and final report (Closes: none; Risk mitigation: none)
- [ ] Create Detection Engineering Skill integration route: detect when Detection Engineering Skill calls `validateRule()`, auto-populate emulation inputs from rule schema, gate behind `detectionEmulation.skillComposition` feature flag (Closes: none; Risk mitigation: none)
- [ ] Write integration test: Detection Engineering Skill calls Detection Emulation Skill API, receives ValidationReport, incorporates score into rule-promotion gate (Closes: none; Risk mitigation: none)
- [ ] Document Detection Engineering Skill integration in user-facing README (Closes: none; Risk mitigation: none)

## Phase 4 — Migration Validator Integration

- [ ] Implement migration-validator trigger: when Splunk, QRadar, or SIGMA rule migration completes, invoke `validateRule()` as part of promotion pipeline if `detectionEmulation.migrationValidator` flag is enabled (Closes: none; Risk mitigation: none)
- [ ] Implement gating logic: block rule promotion if confidence score falls below operator-configurable threshold (default 75), surface decision and remediation steps in migration workflow (Closes: none; Risk mitigation: none)
- [ ] Write integration test: migrate a Splunk rule, trigger Detection Emulation, assert promotion gate gates on low confidence score (Closes: none; Risk mitigation: none)
- [ ] Document migration-validator integration in user-facing README (Closes: none; Risk mitigation: none)
