import type { ToolingLog } from '@kbn/tooling-log';
import type { StartServerOptions } from './flags';
export declare function startServers(log: ToolingLog, options: StartServerOptions): Promise<void>;
